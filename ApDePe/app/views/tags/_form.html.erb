<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibió a esta etiqueta de ser guardada:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div id="HeaderAutor"><%= f.label :Nombre %></div>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Crear etiqueta'%>
  </div>
<% end %>
